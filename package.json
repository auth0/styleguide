{
  "name": "auth0-styleguide",
  "version": "5.0.0",
  "scripts": {
    "heroku-postbuild": "lerna bootstrap",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "build": "lerna run build",
    "publish:cdn": "lerna run publish:cdn",
    "precommit": "lint-staged"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/auth0/styleguide"
  },
  "license": "UNLICENSED",
  "author": "Auth0 (https://auth0.com)",
  "engines": {
    "npm": "^3.10.8"
  },
  "dependencies": {
    "lerna": "2.0.0-beta.38"
  },
  "devDependencies": {
    "husky": "^0.13.4",
    "lint-staged": "^3.6.0",
    "prettier": "^1.4.2"
  },
  "private": true,
  "lint-staged": {
    "*.js": [
      "prettier --write --print-width 100 --single-quote",
      "git add"
    ]
  }
}
