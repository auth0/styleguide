grid(cols = 1, gutter = 0, childs = ".item", orientation= "right", autoWidth = true)
  clearfix()

  if autoWidth
    width: auto

  margin-right: -(gutter)

  {childs}
    float: left
    width: (100% / cols)
    padding-{orientation}: gutter
    background-clip: content-box
