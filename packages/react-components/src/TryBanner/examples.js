import React from 'react';
import TryBanner from './';

const examples = [
  {
    component: <TryBanner />,
    code: '<TryBanner />',
    title: 'Default',
    url: 'default'
  }
];

export default examples;
