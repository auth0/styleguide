@import '../../../../../core/src/vars'
@import '../../../../../core/src/mixins'

.auth0-react-styleguide

  > .a0r-sidebar
    position: fixed;

    +breakpoint("tablet", "max")
      top: 0;

  .styleguide-content
    overflow-y: auto;
    margin-top: 70px;

    +breakpoint("tablet")
      margin-top: 0;
      margin-left: 250px;